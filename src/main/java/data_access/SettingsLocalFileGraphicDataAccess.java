package data_access;

/**
 * Implements the DataAccessInterface. T
 * This class is called by the ManagerInteractor to physically read from and write to the saved file.
 * LocalFile access btw
 * This is for customization. might change it to take from profile later
 * The Implementation of the contract (the code that touches the file).Handling external technologies (DBs, files, UI frameworks).
 * Handling external technologies (DBs, files, UI frameworks).
 */
public class SettingsLocalFileGraphicDataAccess {
}
